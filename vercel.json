{
  "version": 2,
  "buildCommand": "cd react-app && npm install && CI=false npm run build && mkdir -p ../app/static && cp -r build/. ../app/static/",
  "functions": {
    "api/index.py": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.py"
    }
  ]
}
